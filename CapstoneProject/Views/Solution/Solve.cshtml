@model IEnumerable<CapstoneProject.Models.Client>
@*@model CapstoneProject.Models.Client*@

@{
    ViewBag.Title = "Solve";
}

<h2>Solve</h2>

<table class="table table-striped">
    <tr>
        <td>Client Test Name</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            
            <td>@item.FirstName</td>
            <td>@item.AvailableFrom</td>
        </tr>
    }
</table>

